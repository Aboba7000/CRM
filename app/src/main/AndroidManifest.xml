<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.crm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddClientActivity"></activity>
        <activity android:name=".CrossAnalyzeActivity" />
        <activity android:name=".XyzAnalyzeActivity" />
        <activity android:name=".AbcAnalyzeActivity" />
        <activity android:name=".SellsDynamicAnalyzeActivity" />
        <activity android:name=".ClientsReportActivity" />
        <activity android:name=".SellsReportActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".SupportActivity" />
        <activity
            android:name=".ClientActivity"
            android:label="@string/title_activity_client"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>